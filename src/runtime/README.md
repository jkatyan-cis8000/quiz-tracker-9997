# Runtime Layer

Application lifecycle and orchestration.

Wires together all components and manages the application runtime.
May only import from types, config, repo, service, providers, and runtime layers.
