# Utils Layer

Pure helper functions only - no domain logic or internal imports.

Contains utility functions like parse_int and format_score that are reusable.
May only import from utils layer (no internal module imports).
