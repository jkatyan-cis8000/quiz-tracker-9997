# Providers Layer

Cross-cutting concerns - logging, auth, telemetry, connectors.

Contains utility providers like Logger that are used across layers.
May only import from types, config, utils, and providers layers.
